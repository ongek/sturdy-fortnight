name: Build MilkyWay macOS Native

on:
  push:
  workflow_dispatch:

jobs:
  build-test:
    runs-on: macos-26
    steps:
      - name: Show System Info & Clang Version
        run: |
          uname -a
          arch
          clang --version

      - name: Install Dependencies
        run: |
          brew install libomp cmake autoconf automake libtool

      - name: Configure Git for Legacy Submodules
        run: |
          git config --global url."https://".insteadOf git://

      - name: Clone MilkyWay@home Source
        run: |
          git clone --depth 1 --recurse-submodules https://github.com/Milkyway-at-home/milkywayathome_client.git source

      - name: Build BOINC Libraries (arm64)
        run: |
          cd source/boinc/boinc
          mkdir -p "$HOME/bin"
          ln -sf "$(which glibtoolize)" "$HOME/bin/libtoolize"
          export PATH="$HOME/bin:$PATH"

          ./_autosetup
          ./configure \
            --disable-server \
            --disable-client \
            --disable-manager \
            --enable-libraries \
            --disable-shared \
            --enable-static \
            CFLAGS="-O3 -arch arm64 -flto=thin -Wno-error" \
            CXXFLAGS="-O3 -arch arm64 -flto=thin -Wno-error"

          make -C lib -j$(sysctl -n hw.ncpu)
          make -C api -j$(sysctl -n hw.ncpu)

      - name: Configure & Patch MilkyWay CMake (M4 Extreme Tuning)
        run: |
          cd source
          sed -i '' 's/cmake_minimum_required(VERSION [0-9.]*)/cmake_minimum_required(VERSION 3.5)/g' CMakeLists.txt
          sed -i '' 's/message(FATAL_ERROR.*OS X SDK.*/# bypass ancient sdk check/g' cmake_modules/SpecialLibs.cmake

          mkdir -p build && cd build
          SYSROOT=$(xcrun --show-sdk-path)
          CLANG_PERMISSIVE="-Wno-incompatible-function-pointer-types -Wno-int-conversion -Wno-implicit-function-declaration -Wno-deprecated-non-prototype -Wno-error"

          # 【M4極限最適化フラグ群】
          # 1. -mcpu=apple-m4: M4のパイプライン特性を直撃
          # 2. -flto=thin: リンク時最適化（ファイル境界を越えたインライン化）
          # 3. -falign-functions=128: M4の128バイトキャッシュラインにコードを整列
          # 4. -funroll-loops: 10ワイド超幅広デコードを埋めるループ展開
          # 5. -ffp-contract=fast: ハードウェアFMA（積和演算器）を全開駆動
          # 6. -mmacosx-version-min=15.0: 2007年のLeopard互換を脱却し最新ABIを適用
          M4_OPT="-O3 -mcpu=apple-m4 -flto=thin -falign-functions=128 -funroll-loops -ffp-contract=fast -fno-math-errno -mmacosx-version-min=15.0"

          cmake .. \
            -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
            -DCMAKE_POLICY_DEFAULT_CMP0074=NEW \
            -DCMAKE_OSX_SYSROOT="$SYSROOT" \
            -DCMAKE_OSX_DEPLOYMENT_TARGET="15.0" \
            -DBOINC_DIR=../boinc/boinc \
            -DBOINC_APPLICATION=ON \
            -DNBODY=ON \
            -DSEPARATION=OFF \
            -DNBODY_OPENMP=ON \
            -DNBODY_GL=OFF \
            -DOpenMP_ROOT=/opt/homebrew/opt/libomp \
            -DOpenMP_C_FLAGS="-Xpreprocessor -fopenmp -I/opt/homebrew/opt/libomp/include" \
            -DOpenMP_CXX_FLAGS="-Xpreprocessor -fopenmp -I/opt/homebrew/opt/libomp/include" \
            -DOpenMP_C_LIB_NAMES="omp" \
            -DOpenMP_CXX_LIB_NAMES="omp" \
            -DOpenMP_omp_LIBRARY=/opt/homebrew/opt/libomp/lib/libomp.dylib \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_FLAGS="$M4_OPT -I/opt/homebrew/opt/libomp/include $CLANG_PERMISSIVE" \
            -DCMAKE_CXX_FLAGS="$M4_OPT -I/opt/homebrew/opt/libomp/include $CLANG_PERMISSIVE"

      - name: Compile & Link N-body (with link.txt Surgery & ThinLTO)
        run: |
          cd source/build

          # コンパイル実行（オブジェクトファイル生成）
          make -j$(sysctl -n hw.ncpu) || true

          TARGET_LINK="nbody/CMakeFiles/milkyway_nbody.dir/link.txt"
          if [ -f "$TARGET_LINK" ]; then
            echo "=== Patching $TARGET_LINK ==="
            # 不正オプションの消去
            sed -i '' 's/-Wl,--start-group//g; s/-Wl,--end-group//g; s/--start-group//g; s/--end-group//g' "$TARGET_LINK"
            sed -i '' 's/-libstdc++//g; s/-static//g' "$TARGET_LINK"
            
            # 古い10.5ターゲットを消し、最新の15.0とThinLTO、OpenMPをリンクに注入
            sed -i '' 's/-mmacosx-version-min=[0-9.]*/-mmacosx-version-min=15.0/g' "$TARGET_LINK"
            sed -i '' 's|$| -flto=thin -L/opt/homebrew/opt/libomp/lib -lomp|' "$TARGET_LINK"

            echo "=== Executing Tuned Link Command ==="
            (cd nbody && bash -c "$(cat CMakeFiles/milkyway_nbody.dir/link.txt)")
          fi

          echo "=== Built Binaries ==="
          ls -lh bin/* || ls -lh nbody/* || ls -lh *milkyway* || true
          file bin/milkyway_nbody || file nbody/milkyway_nbody || true

      - name: Upload Native Binary
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: milkyway_nbody_m4_extreme
          path: |
            source/build/bin/
            source/build/nbody/
